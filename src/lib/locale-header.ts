/**
 * Заголовок запроса, в котором прокси называет локаль пути.
 *
 * Читатель РОВНО один — `src/app/global-not-found.tsx`: у страницы 404 нет
 * ни `params`, ни адреса, и узнать язык человека ей больше негде (долг
 * 129). Именно поэтому заголовок заведён снова: прежний `x-pathname` был
 * снят в 7.х как «заголовок без читателя», и возвращать его форму — значит
 * возвращать и ту причину. Здесь читатель есть, и он один.
 *
 * В `[lang]/layout.tsx` и в `generateMetadata` его читать НЕЛЬЗЯ: чтение
 * `headers()` в layout'е выключает статический рендер всему поддереву —
 * ровно та причина, по которой `x-pathname` и был убран.
 */
export const LOCALE_HEADER = "x-rf-locale";

/**
 * Сегмент, на который прокси переписывает адрес, у которого своей страницы
 * нет (испанский маршрут под `/ru`, долг 127). Он нарочно не совпадает ни с
 * одним маршрутом: смысл переписывания — попасть на тот же внутренний
 * маршрут 404, которым отвечает опечатка в адресе, и получить нашу
 * страницу вместо пустой. В строке браузера остаётся исходный адрес.
 */
export const NOT_FOUND_REWRITE_SEGMENT = "__not-found__";
