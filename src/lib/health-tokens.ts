/**
 * Слова, по которым внешний монитор отличает живой ответ `/api/health` от
 * любого другого.
 *
 * Отдельный файл, а не пара `export const` внутри самого маршрута, по
 * технической причине: файл `route.ts` в App Router умеет экспортировать
 * только имена, известные Next.js (`GET`, `dynamic`, `runtime` и т. п.), и
 * любое лишнее имя роняет сборку на типах — проверено 17.09.2026, сборка
 * упала строкой `Property 'HEALTH_OK_TOKEN' is incompatible with index
 * signature`.
 *
 * НАПИСАНИЕ ВЫБРАНО НЕ СЛУЧАЙНО. Монитор ищет ключевое слово ПОДСТРОКОЙ,
 * поэтому пара «healthy / unhealthy» была бы ловушкой: «unhealthy»
 * содержит «healthy», и больной сайт считался бы здоровым. Здесь у двух
 * слов нет ни одного общего вхождения, и это держит сторож
 * `npm run check:health-route`.
 */

/** Живой ответ. Это же слово вписывается в поле «Keyword» UptimeRobot. */
export const HEALTH_OK_TOKEN = "RF-OK";

/** Отказ. Нужен, чтобы в журнале монитора было видно: ответ пришёл от
 *  НАС, а не от прослойки CDN или от чужой заглушки. */
export const HEALTH_DOWN_TOKEN = "RF-DOWN";
