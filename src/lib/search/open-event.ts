/**
 * Имя события «открой окно поиска».
 *
 * У поиска на этом сайте НЕТ адреса: `?q=` в строку браузера не пишется
 * намеренно (`src/components/GlobalSearch.tsx`, и запись про отсутствие
 * `potentialAction: SearchAction` в `src/lib/site.ts`). Значит ссылку на
 * поиск нельзя сделать ссылкой — её можно сделать только кнопкой, которая
 * говорит окну в шапке «откройся».
 *
 * Событие, а не проброшенный колбэк, потому что шапка и страница 404 стоят
 * в дереве рядом, а не одна внутри другой, и общего состояния у них нет.
 * Разметка при этом не меняется ни на одной странице: `GlobalSearch`
 * только подписывается, ни одного нового узла в DOM не появляется.
 */
export const SEARCH_OPEN_EVENT = "rf:open-search";
